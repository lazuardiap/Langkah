<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.newshelter.langkah">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Langkah"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".ui.consultation.ConsultationResultActivity"/>
        <activity android:name=".ui.consultation.ConsultationActivity" />
        <activity android:name=".ui.hospital.LocationActivity" />
        <activity android:name=".ui.profile.ChangePasswordActivity" />
        <activity android:name=".ui.profile.EditProfileActivity" />
        <activity android:name=".ui.room.RoomDetailActivity" />
        <activity android:name=".ui.polyclinics.PolyclinicDetailsActivity" />
        <activity android:name=".ui.doctors.DoctorDetailsActivity" />
        <activity android:name=".ui.hospital.HospitalDetailActivity" />
        <activity android:name=".AppActivity" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCnUGX6sgPdPUoWChrvY2bqyqyMhD76-PM" />

        <activity
            android:name=".LandingActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Langkah">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>